function setup() {
  createCanvas(500, 500);
}

function draw() {
  background(20);
  for (var x = 10; x <= 490; x = x + 10) {
    for (var y = 10; y <= 490; y = y + 10)
      ellipse(x, y, 10, 10);
    fill(0, 0, 255);
  }
}
